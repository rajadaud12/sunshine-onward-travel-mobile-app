import 'package:flutter/material.dart';

/// Centralized color palette for Sunshine Onward Travel.
class AppColors {
  // Brand
  static const Color primary = Color(0xFFF52D56);

  // Text
  static const Color textSecondary = Color(0xFF656565);

  // Surfaces
  static const Color border = Color(0xFFEDEDED);
  static const Color card = Color(0xFFFAFAFA);
  static const Color primarySoft = Color(0xFFF2CBD3);
  static const Color success = Color(0xFF67D586);
  static const Color waypoint = Colors.purple;

  // Placeholders
  static const Color placeholder = Color(0xFFAAAAAA);

  // Utilities
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
